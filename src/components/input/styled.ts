import styled from "styled-components";

export const Input = styled.input`
  max-width: 200px;
  font-size: 16px;
  padding: 10px;
  outline: none;
  color: var(--color-h2);
  border: 2px solid var(--color-h2);
`;
