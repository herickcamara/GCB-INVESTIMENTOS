import styled from "styled-components";

export const Container = styled.div`
  display: grid;
  grid-template-columns: 1fr;
  gap: 15px;
  h2 {
    font-size: 28px;
    color: var(--color-h2);
  }
`;
